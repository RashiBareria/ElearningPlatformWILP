CREATE TABLE dbo.Lesson (
    LessonID INT IDENTITY(1,1),
    CourseID INT NOT NULL,
    LessonTitle NVARCHAR(255) COLLATE SQL_Latin1_General_CP1_CI_AS NOT NULL,
    Content TEXT NOT NULL,
    VideoUrl NVARCHAR(255) COLLATE SQL_Latin1_General_CP1_CI_AS NULL,
    CreationDate DATETIME NOT NULL DEFAULT GETDATE()
)
GO
ALTER TABLE dbo.Lesson
	ADD CONSTRAINT [PK_Lesson] PRIMARY KEY CLUSTERED (LessonID) ON [PRIMARY]
GO
ALTER TABLE dbo.Lesson
	ADD CONSTRAINT [FK_Lesson_Course] FOREIGN KEY (CourseID) REFERENCES dbo.Course(CourseID)
GO